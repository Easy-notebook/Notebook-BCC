<behavior id="load_dataset_b1" step_id="load_dataset">
  <agent>DefineAgent</agent>
  <task>Use pandas to load the dataset from the provided file_path into a DataFrame named df_raw. Ensure the CSV has valid headers, consistent column types, and no structural issues. Store df_raw in memory for downstream processing.</task>
  <inputs>
    <variable name="file_path">./assets/housing.csv</variable>
  </inputs>
  <effects>False</effects>
  <outputs>
    <artifact name="df_raw">Raw DataFrame loaded from housing.csv, type: pandas.DataFrame, must be non-empty with valid numeric and categorical columns.</artifact>
  </outputs>
  <acceptance>
    <criterion>os.path.exists("./assets/housing.csv")==True</criterion>
    <criterion>df_raw.shape[0] > 0</criterion>
    <criterion>len(df_raw.columns) > 1</criterion>
  </acceptance>
  <whathappened>
    <overview>The current stage focuses on defining and verifying dataset existence and structure to ensure the problem definition is grounded on valid data.</overview>
    <background>No prior execution effects were detected; this is the initial data loading step of the problem_definition stage.</background>
  </whathappened>
</behavior>